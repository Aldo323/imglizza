# Reemplazar .gitignore con versión corregida
@"
# Imágenes originales (muy pesadas)
originales/
Anillos/*.png

# Logs y temporales
*.log
__pycache__/
.DS_Store
Thumbs.db

# Permitir imágenes optimizadas
# (no bloquear *.webp ni *.jpg globalmente)
"@ | Out-File -FilePath ".gitignore" -Encoding UTF8 -Force